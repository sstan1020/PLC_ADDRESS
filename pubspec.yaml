name: FLUTTER_ASI
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  cupertino_icons: ^1.0.2
  path: ^1.8.3
  json_annotation: ^4.9.0  # 升级到最新稳定版
  collection: ^1.15.0
  meta: ^1.7.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.4
  json_serializable: ^6.7.1  # 确保与 json_annotation 兼容

FLUTTER_ASI:
  uses-material-design: true
  assets:
    - lib/config.json
